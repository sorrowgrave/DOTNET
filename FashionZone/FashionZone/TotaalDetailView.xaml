<Controls:MetroWindow x:Class="FashionZone.TotaalDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FashionZone"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="TotaalDetailView" Height="300" Width="507.276"
        GlowBrush="{DynamicResource AccentColorBrush}">
    <Grid>
        <DataGrid x:Name="totalenDataGrid" HorizontalAlignment="Left" Margin="79,35,0,0" VerticalAlignment="Top" Height="178" Width="325">
            <DataGrid.Columns>
                <DataGridTextColumn Width="100" Header="Merk" Binding="{Binding Artikelnr}"/>
                <DataGridTextColumn Width="110" Header="Voorzien" Binding="{Binding Artikelnr}"/>
                <DataGridTextColumn Width="110" Header="Besteedt" CanUserResize="False" Binding="{Binding Artikelnr}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="closeButton" Content="Close" HorizontalAlignment="Left" Margin="393,226,0,0" VerticalAlignment="Top" Width="75" Click="closeButton_Click"/>
    </Grid>
</Controls:MetroWindow>
